[tool:pytest]
testpaths = backend/tests
python_paths = .
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::MovedIn20Warning 